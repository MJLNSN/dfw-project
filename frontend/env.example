# DFW Property Search Platform - Frontend Environment Variables
# Copy this file to .env.local and update values as needed
#
# ⚠️ IMPORTANT: Never commit .env.local files to Git!
# All values prefixed with VITE_ are exposed to the browser.

# ===========================================
# BACKEND API CONFIGURATION (Optional)
# ===========================================
# URL of the backend API server
# Default: http://localhost:8000 for local development
VITE_API_URL=http://localhost:8000

# ===========================================
# MAPBOX CONFIGURATION (Required)
# ===========================================
# Mapbox public access token for map rendering
# Get your token from: https://account.mapbox.com/access-tokens/
#
# Required token scopes:
#   - styles:tiles (read tile data)
#   - styles:read (read style data)
#   - fonts:read (read font data)
#   - datasets:read (read datasets)
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# ===========================================
# AWS COGNITO CONFIGURATION (Required)
# ===========================================
# These must match your AWS Cognito User Pool settings
# Used for user registration and authentication

# AWS Region where Cognito is deployed
VITE_AWS_REGION=us-east-2

# Cognito User Pool ID (format: region_xxxxx)
VITE_COGNITO_USER_POOL_ID=us-east-2_XXXXXXXXX

# Cognito App Client ID (from App integration > App clients)
# Must be a client WITHOUT a client secret (public client)
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
