# DFW Property Search Platform - Backend Environment Variables
# Copy this file to .env and update values as needed
#
# ⚠️ IMPORTANT: Never commit .env files to Git!
# All values below are REQUIRED unless marked as optional.

# ===========================================
# DATABASE CONFIGURATION (Required)
# ===========================================
# PostgreSQL connection string with PostGIS support
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://takehome:YOUR_PASSWORD@108.61.159.122:13432/gis

# ===========================================
# AWS COGNITO CONFIGURATION (Required)
# ===========================================
# These are used for JWT token verification
# Get these values from AWS Cognito Console

# AWS Region where Cognito is deployed
AWS_REGION=us-east-2

# Cognito User Pool ID (format: region_xxxxx)
COGNITO_USER_POOL_ID=us-east-2_XXXXXXXXX

# Cognito App Client ID (from App integration > App clients)
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# CORS CONFIGURATION (Optional)
# ===========================================
# Comma-separated list of allowed origins for CORS
# Default includes localhost for development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend-domain.com

# ===========================================
# APPLICATION CONFIGURATION (Optional)
# ===========================================
# Enable debug mode (True/False)
DEBUG=True

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
